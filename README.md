# pit-abuse-reporter
A lightweight Thunderbird add-on that lets you quickly report abuse for unsolicited emails. It extracts the sender’s domain, finds the correct abuse address (via DMARC when available), and opens a ready-to-send report with message headers and raw content attached.
